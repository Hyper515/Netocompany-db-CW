html
  head
    h1.title-company Netocompany
    h1 Edit Patient
    link(rel='stylesheet' href='/style.css')
  body
    form(action=`/PatientEditPage/${patient.NhsNo}` method='post')
      label(for='forename') Forename:
      input(type='text' name='name' value=patient.Forename)
      div
      label(for='surname') Surname:
      input(type='text' name='name' value=patient.Surname)
      div
      label(for='dob') Date Of Birth:
      input(type='date' name='age' value=patient.Dob)
      div
      label(for='gender') Gender:
      select(name='gender')
        option(value='male' selected=patient.Gender === 'male') M
        option(value='female' selected=patient.Gender === 'female') F
      div
      label(for='address') Address:
      input(type='text' name='address' value=patient.Address)
      div
      label(for='postcode') Postcode:
      input(type='text' name='postcode' value=patient.Postcode)
      div
      label(for='phone') Phone:
      input(type='text' name='phone' value=patient.MobNo)
      div
      button(type='submit') Save
      button.button-format(type='button', onclick='window.location.href="/patients";') Back
